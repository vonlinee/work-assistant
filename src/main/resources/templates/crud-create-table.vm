-- ============================================================
-- CREATE TABLE script for: ${tableName}
-- Generated by work-assistant CRUD Generator
-- Author: ${author}
-- Date:   ${date}
-- ============================================================

CREATE TABLE IF NOT EXISTS ${tableName} (
#foreach($col in $columns)
    ${col.columnName} ${col.jdbcType}#if($col.columnSize > 0 && ($col.jdbcType == "VARCHAR" || $col.jdbcType == "CHAR"))(${col.columnSize})#end#if($col.primaryKey) PRIMARY KEY AUTO_INCREMENT#end#if(!$col.nullable && !$col.primaryKey) NOT NULL#end#if($col.comment && $col.comment != "") COMMENT '${col.comment}'#end#if($foreach.hasNext),#end

#end
);
