package ${package}.entity;

#if($useLombok)
import lombok.Data;
import lombok.Builder;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
#end
#if($useSwagger)
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
#end
import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;

import java.io.Serializable;

/**
 * ${className} entity.
 * Table: ${tableName}
 *
 * @author ${author}
 * @date ${date}
 */
#if($useLombok)
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
#end
#if($useSwagger)
@ApiModel(value = "${className}", description = "Entity for table ${tableName}")
#end
@TableName("${tableName}")
public class ${className}#if($entitySuper && $entitySuper != "") extends ${entitySuper}#end implements Serializable {

    private static final long serialVersionUID = 1L;

#foreach($col in $columns)
#if($col.comment && $col.comment != "")
    /** ${col.comment} */
#end
#if($useSwagger && $col.comment && $col.comment != "")
    @ApiModelProperty("${col.comment}")
#end
#if($col.primaryKey)
    @TableId(type = IdType.AUTO)
#end
    private ${col.javaType} ${col.javaFieldName};

#end
#if(!$useLombok)
    // ── Getters & Setters ──

#foreach($col in $columns)
    public ${col.javaType} get${col.capitalizedFieldName}() {
        return ${col.javaFieldName};
    }

    public void set${col.capitalizedFieldName}(${col.javaType} ${col.javaFieldName}) {
        this.${col.javaFieldName} = ${col.javaFieldName};
    }

#end
#end
}
